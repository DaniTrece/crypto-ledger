[project]
name = "crypto-ledger"
version = "0.1.0"
description = "Crypto portfolio & tax tracker for Spain"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
]
